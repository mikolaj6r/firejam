# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on push to main
"on":
  push:
    branches:
      - main
jobs:
  build_and_deploy_admin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: "12"
      - run: yarn install
      - run: yarn run build-admin
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_FIREJAM_EF3DF }}"
          channelId: live
          projectId: firejam-ef3df
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
  build_and_deploy_functions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: "8"
      - run: yarn install
      - run: yarn run build-functions
      - name: Deploy to Firebase Functions
        uses: w9jds/firebase-action@v2.0.0
        with:
          args: deploy --only functions
        env:
          GCP_SA_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_FIREJAM_EF3DF }}
